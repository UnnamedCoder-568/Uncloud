# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vendored uv binaries (~50 MB each), staged per platform at build time by
# scripts/stage_uv.sh. The README beside them is tracked; the binaries are not.
src-tauri/binaries/uv
src-tauri/binaries/uv-aarch64
src-tauri/binaries/uv-x86_64
