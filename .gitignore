# ---------------------------------------------------------------- weights
# Model files are enormous and belong on disk, not in git.
models/
New Models/
*.gguf
*.safetensors
*.bin
*.pt
*.ckpt

# ------------------------------------------------------------ dependencies
node_modules/
.venv/
venv/
__pycache__/
*.py[cod]
.uv-cache/

# ----------------------------------------------------------------- builds
dist/
dist-installers/
build/
target/
*.dmg
*.app/
*.AppImage
*.deb
*.msi
*.exe

# ------------------------------------------------------- legacy prototypes
# Superseded by uncloud/ + sidecar/; kept on disk, out of the repo.
app/
frontend/
web/
workspace/
Astro Mark 1/
/src-tauri/
server.py
# Anchored. Unanchored, this matched docs/ at ANY depth and silently swallowed
# uncloud/docs/ — only the legacy prototype at the root is meant to be ignored.
/docs/
.aider*
.continue/
1ls
TestPrint.py
Option 1*

# --------------------------------------------------- fetched at build time
# uv is downloaded per-platform by CI; ~50 MB, never committed.
#
# binaries/ holds exactly ONE uv — the one matching the target being built —
# because tauri.conf globs binaries/* into the bundle. The per-architecture
# copies are staged from vendor/ and must live outside binaries/, or every
# build would ship all of them.
uncloud/src-tauri/binaries/uv
uncloud/src-tauri/binaries/uv.exe
uncloud/src-tauri/vendor/

# ------------------------------------------------------------------- local
.DS_Store
.env
.env.*
*.log
.idea/
.vscode/

# `models/` above keeps downloaded weights out of git. It also matched Core's
# source package for identifying models, which is code and must be committed.
!sidecar/uncloud_engine/core/models/

# Staged native Windows chat runtime
uncloud/src-tauri/binaries/llama/
